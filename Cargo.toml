[package]
name = "greatest_common_divisor"
version = "0.1.0"
edition = "2018"
authors = ["LJason <ljason@ljason.cn>"]
readme = "README.md"

[profile.release]
codegen-units = 1
lto = "fat"
panic = "abort"

[dependencies]

[dev-dependencies]
rand = "0.8"
